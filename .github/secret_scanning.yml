# alltest is a secret scanner — its test fixtures and regression tests deliberately
# contain fake, non-functional secret patterns (whsec_, xapp-, ghp_, AKIA…) so the probes
# have something to detect. These are NOT real credentials. Exclude them from GitHub secret
# scanning so they don't raise false "exposed secret" alerts.
paths-ignore:
  - "test/**"
